# GitHub App Configuration
GITHUB_APP_ID=GH App ID
GITHUB_PRIVATE_KEY_PATH=/path/to/key.pem
GITHUB_TOKEN="GH Token"

# Webhook Configuration
WEBHOOK_SECRET=GH Webhook Token

# Server Configuration
PORT=3000
NODE_ENV=production

# Azure OpenAI Configuration (Optional - for AI-powered scorecard analysis)
# Endpoint URL for your Azure OpenAI resource
AZURE_OPENAI_ENDPOINT="https://your-resource.openai.azure.com/"
# Deployment name for your model (e.g., gpt-4, gpt-35-turbo)
AZURE_OPENAI_DEPLOYMENT_NAME="gpt-4"
# API version (optional, defaults to 2024-10-21)
AZURE_OPENAI_API_VERSION="2024-10-21"
# API key (optional - if not provided, uses Azure CLI or Managed Identity authentication)
AZURE_OPENAI_API_KEY="your-api-key-if-not-using-managed-identity"

# For local development, you can use Azure CLI authentication:
# 1. Install Azure CLI: https://docs.microsoft.com/en-us/cli/azure/install-azure-cli
# 2. Run: az login
# 3. Leave AZURE_OPENAI_API_KEY empty - the service will use Azure CLI credentials

# For production deployment, use Managed Identity:
# 1. Enable system-assigned or user-assigned managed identity on your Azure resource
# 2. Grant the identity "Cognitive Services OpenAI User" role on your Azure OpenAI resource
# 3. Leave AZURE_OPENAI_API_KEY empty - the service will use managed identity

# Legacy Azure AI Foundry (deprecated - replaced by Azure OpenAI)
PROJECT_ENDPOINT="Azure AI Foundry Endpoint"
MODEL_DEPLOYMENT_NAME="gpt-5-mini"